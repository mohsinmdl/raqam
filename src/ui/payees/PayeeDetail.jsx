// src/ui/payees/PayeeDetail.jsx — placeholder body; Task 8 replaces this with
// the real single-payee editor (rename, hide, auto-categorize, rename rules).
export default function PayeeDetail({ entry, onDeselect }) {
  return <div style={{ padding: 24, fontSize: 13.5 }}>{entry.name}</div>;
}
